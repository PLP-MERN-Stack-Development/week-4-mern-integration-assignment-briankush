{
  "name": "mern-blog-fullstack",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --prefix server run dev\" \"pnpm --prefix client run dev\"",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.11"
  }
}
